{
  "name": "lwlawson-portfolio.web-app",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng build && ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true
}
